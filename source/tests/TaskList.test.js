test('TaskList.addTask(task) stores the specified task in local storage', () => {

});

test('TaskList.removeTask(taskId) removes the specified task from local storage', () => {

});

test('TaskList.markTaskAsComplete(taskId) marks the specified task as complete and updates the current task', () => {

});

test('TaskList.incrementPomodoroCount(taskId) increases the specified task\'s pomodoro count', () => {

});
